# VS Code Remote SSH Port Forwarding 原理說明

## 📚 目錄

- [概述](#概述)
- [架構原理](#架構原理)
- [工作流程](#工作流程)
- [實際應用](#實際應用)
- [驗證方法](#驗證方法)
- [故障排除](#故障排除)

---

## 🎯 概述

當使用 **VS Code Remote SSH** 連接到遠端 Linux 伺服器時,可以在本地瀏覽器使用 `http://localhost:3005` 訪問遠端服務。這是通過 **SSH Port Forwarding (端口轉發)** 技術實現的。

### 核心概念

- **本地開發機**: Windows/macOS 運行 VS Code
- **遠端伺服器**: Linux 運行實際服務 (10.64.11.49)
- **SSH Tunnel**: 加密通道連接兩端
- **Port Forwarding**: 自動將本地端口映射到遠端端口

---

## 🏗️ 架構原理

### 完整架構圖

```
┌─────────────────────────────────────────────────────────────────┐
│                        本地 Windows/macOS                        │
│                                                                  │
│  ┌──────────────┐          ┌─────────────────────────────┐    │
│  │   瀏覽器      │          │      VS Code Editor         │    │
│  │              │          │                             │    │
│  │ localhost    │◄────────►│  Port Forwarding 管理器      │    │
│  │ :3005        │   HTTP   │  ┌─────────────────────┐   │    │
│  │ :8085        │          │  │ Ports 面板          │   │    │
│  └──────────────┘          │  │ - 3005 → Remote    │   │    │
│                             │  │ - 8085 → Remote    │   │    │
│                             │  └─────────────────────┘   │    │
│                             └─────────────┬───────────────┘    │
└───────────────────────────────────────────┼────────────────────┘
                                            │
                                    ┌───────▼────────┐
                                    │  SSH Tunnel    │
                                    │  (Port 22)     │
                                    │  加密傳輸       │
                                    └───────┬────────┘
                                            │
┌───────────────────────────────────────────▼────────────────────┐
│                      遠端 Linux (10.64.11.49)                   │
│                                                                  │
│  ┌──────────────────┐         ┌──────────────────┐            │
│  │  Frontend        │         │   Backend        │            │
│  │  serve           │         │   Node.js        │            │
│  │  0.0.0.0:3005    │         │   0.0.0.0:8085   │            │
│  └──────────────────┘         └──────────────────┘            │
│                                                                  │
│  ┌──────────────────────────────────────────────┐              │
│  │  Playwright Browser (在遠端運行)             │              │
│  │  需要訪問: localhost:3005 或 10.64.11.49:3005│              │
│  └──────────────────────────────────────────────┘              │
└──────────────────────────────────────────────────────────────────┘
```

---

## ⚙️ 工作流程

### 1. VS Code 自動偵測端口

當遠端服務啟動並監聽端口時:

```bash
# 遠端 Linux 執行
$ npm run dev

Frontend: 0.0.0.0:3005  ✅ 監聽所有網路介面
Backend:  0.0.0.0:8085  ✅ 監聽所有網路介面
```

VS Code 會:
1. **偵測** 遠端程序正在監聽的端口
2. **提示** "檢測到新的轉發端口"
3. **自動建立** SSH tunnel

### 2. SSH Tunnel 建立

VS Code 在背景執行類似命令:

```bash
# 本地端口 → 遠端端口的映射
ssh -L 3005:localhost:3005 user@10.64.11.49
ssh -L 8085:localhost:8085 user@10.64.11.49
```

**參數說明**:
- `-L`: Local Port Forwarding
- `3005`: 本地端口
- `localhost:3005`: 遠端目標地址和端口
- `user@10.64.11.49`: SSH 連接資訊

### 3. 請求轉發流程

#### 本地瀏覽器訪問流程

```
步驟 1: 本地瀏覽器發送請求
  ↓
  http://localhost:3005/
  ↓
步驟 2: VS Code 攔截請求
  ↓
  檢查 Ports 轉發表
  ↓
步驟 3: 通過 SSH Tunnel 轉發
  ↓
  加密傳輸 (Port 22)
  ↓
步驟 4: 遠端 Linux 接收
  ↓
  127.0.0.1:3005 → Frontend 服務
  ↓
步驟 5: 響應原路返回
  ↓
  遠端 Linux → SSH → VS Code → 本地瀏覽器
```

---

## 💻 實際應用

### WebSecScan Platform 配置

#### 服務端口配置

| 服務 | 監聽地址 | 本地訪問 | 遠端訪問 |
|:---|:---|:---|:---|
| Frontend | `0.0.0.0:3005` | `localhost:3005` | `10.64.11.49:3005` |
| Backend | `0.0.0.0:8085` | `localhost:8085` | `10.64.11.49:8085` |

#### 前端 API 配置

```javascript
// frontend/public/index.html
// 使用遠端 IP,因為前端頁面會被本地和 Playwright 訪問
const API_BASE_URL = 'http://10.64.11.49:8085';

// 健康檢查
fetch('http://10.64.11.49:8085/health')

// 掃描 API
fetch('http://10.64.11.49:8085/api/v1/scan', {...})
```

#### 後端 CORS 配置

```javascript
// backend/src/server.js
app.use(cors({
  origin: [
    'http://localhost:3005',      // 本地 VS Code Port Forwarding
    'http://10.64.11.49:3005',   // 遠端直接訪問
    'http://127.0.0.1:3005',     // 本地迴環
    'http://10.64.11.49:8085'    // 同源請求
  ],
  credentials: true
}));
```

### 訪問場景對照

#### 場景 1: 本地開發者訪問

```
開發者 Windows Chrome
  ↓
http://localhost:3005
  ↓
VS Code Port Forwarding
  ↓
SSH Tunnel
  ↓
遠端 Linux 10.64.11.49:3005
```

**優點**: 
- ✅ 無需記住遠端 IP
- ✅ 自動加密傳輸
- ✅ 透明代理

#### 場景 2: Playwright 自動化測試

```
Playwright Browser (運行在遠端 Linux)
  ↓
http://10.64.11.49:3005
  ↓
本地網路 (同一台機器)
  ↓
Frontend 服務 0.0.0.0:3005
```

**優點**:
- ✅ 直接本地連接,延遲低
- ✅ 無需經過 SSH tunnel
- ✅ 真實網路環境測試

#### 場景 3: 團隊成員訪問

```
同事瀏覽器
  ↓
http://10.64.11.49:3005
  ↓
公司內網
  ↓
遠端 Linux 10.64.11.49:3005
```

**前提**:
- ✅ 需要防火牆開放端口
- ✅ 需要網路可達
- ✅ 需要 CORS 允許來源

---

## 🔍 驗證方法

### 1. 檢查 VS Code Port Forwarding

**VS Code 介面**:
```
1. 打開 VS Code
2. 按 Ctrl+` 開啟終端
3. 點擊 "PORTS" 標籤
```

你會看到:
```
Ports
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Port    Local Address         Running Process      Origin
3005    localhost:3005        npm                  User Forwarded
8085    localhost:8085        node                 Auto Forwarded
```

### 2. 本地檢查端口監聽 (Windows)

```powershell
# PowerShell
netstat -ano | findstr ":3005"
netstat -ano | findstr ":8085"
```

輸出範例:
```
TCP    127.0.0.1:3005    0.0.0.0:0    LISTENING    15432
TCP    127.0.0.1:8085    0.0.0.0:0    LISTENING    15432
```

**PID 15432** 就是 VS Code 的進程!

### 3. 本地檢查端口監聽 (macOS/Linux)

```bash
# 檢查本地端口
lsof -i :3005
lsof -i :8085

# 檢查 VS Code 進程
ps aux | grep "Code"
```

### 4. 遠端檢查服務運行

```bash
# SSH 到遠端 Linux
ssh user@10.64.11.49

# 檢查端口監聽
sudo lsof -i :3005
sudo lsof -i :8085

# 或使用 netstat
netstat -tlnp | grep ':3005'
netstat -tlnp | grep ':8085'
```

輸出範例:
```bash
tcp  0  0  0.0.0.0:3005  0.0.0.0:*  LISTEN  123456/node
tcp  0  0  0.0.0.0:8085  0.0.0.0:*  LISTEN  123457/node
```

### 5. 測試連通性

#### 本地測試

```bash
# Windows PowerShell
Invoke-WebRequest -Uri http://localhost:3005 -UseBasicParsing
Invoke-WebRequest -Uri http://localhost:8085/health -UseBasicParsing

# macOS/Linux
curl http://localhost:3005
curl http://localhost:8085/health
```

#### 遠端測試

```bash
# SSH 到遠端
curl http://localhost:3005
curl http://10.64.11.49:3005

curl http://localhost:8085/health
curl http://10.64.11.49:8085/health
```

---

## 🛠️ 故障排除

### 問題 1: localhost 無法訪問

**症狀**:
```
ERR_CONNECTION_REFUSED
無法連接到 localhost:3005
```

**排查步驟**:

1. **檢查 VS Code Ports 面板**
   ```
   是否顯示端口轉發?
   狀態是否為 "Active"?
   ```

2. **檢查遠端服務是否運行**
   ```bash
   # SSH 到遠端
   curl http://localhost:3005
   ```

3. **手動添加端口轉發**
   ```
   VS Code → PORTS 面板 → Add Port → 輸入 3005
   ```

4. **重啟 SSH 連接**
   ```
   VS Code → Command Palette (F1)
   → "Remote-SSH: Kill VS Code Server on Host"
   → 重新連接
   ```

### 問題 2: CORS 錯誤

**症狀**:
```javascript
Access to fetch at 'http://10.64.11.49:8085/api/v1/scan' 
from origin 'http://localhost:3005' has been blocked by CORS policy
```

**解決方法**:

```javascript
// backend/src/server.js
app.use(cors({
  origin: [
    'http://localhost:3005',
    'http://10.64.11.49:3005',
    'http://127.0.0.1:3005'
  ],
  credentials: true,
  methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],
  allowedHeaders: ['Content-Type', 'Authorization']
}));
```

### 問題 3: Playwright 無法訪問

**症狀**:
```
TimeoutError: page.goto: Timeout 30000ms exceeded
```

**原因分析**:

Playwright 在**遠端 Linux**運行,它無法使用 VS Code 的 Port Forwarding。

**解決方法**:

```javascript
// 使用遠端 IP 或 localhost (遠端本機)
await page.goto('http://localhost:3005');  // ✅ 推薦
await page.goto('http://10.64.11.49:3005'); // ✅ 也可以
await page.goto('http://127.0.0.1:3005');   // ✅ 也可以
```

### 問題 4: 端口衝突

**症狀**:
```
Error: listen EADDRINUSE: address already in use :::3005
```

**解決方法**:

```bash
# 使用專案提供的清理工具
npm run kill-ports

# 或手動清理
lsof -ti:3005 | xargs kill -9
lsof -ti:8085 | xargs kill -9
```

### 問題 5: 防火牆阻擋

**症狀**:
團隊成員無法通過 `10.64.11.49:3005` 訪問。

**解決方法**:

```bash
# 檢查防火牆
sudo firewall-cmd --list-ports

# 開放端口 (CentOS/RHEL)
sudo firewall-cmd --permanent --add-port=3005/tcp
sudo firewall-cmd --permanent --add-port=8085/tcp
sudo firewall-cmd --reload

# 或使用 iptables (Ubuntu/Debian)
sudo iptables -A INPUT -p tcp --dport 3005 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 8085 -j ACCEPT
sudo iptables-save
```

---

## 📊 性能對比

### 延遲測試

| 訪問方式 | 平均延遲 | 適用場景 |
|:---|:---:|:---|
| 本地 localhost (Port Forwarding) | ~5ms | 開發調試 |
| 遠端本機 localhost | <1ms | Playwright 測試 |
| 遠端 IP 10.64.11.49 | ~2ms | 團隊協作 |
| 公網 IP (若有) | ~50ms | 外部展示 |

---

## 🎓 總結

### 核心要點

1. **VS Code Port Forwarding** 是基於 SSH 的自動端口映射
2. **本地開發者** 使用 `localhost` 透過 SSH tunnel 訪問
3. **Playwright** 在遠端運行,應使用遠端 IP 或本機 localhost
4. **CORS 配置** 需要同時支援多種來源
5. **監聽地址** `0.0.0.0` 允許所有網路介面訪問

### 最佳實踐

✅ **服務啟動**: 使用 `0.0.0.0` 監聽所有介面
✅ **本地開發**: 使用 `localhost` + VS Code Port Forwarding
✅ **自動化測試**: Playwright 使用遠端 IP
✅ **CORS 配置**: 允許 localhost 和遠端 IP
✅ **端口管理**: 使用專案的 `npm run kill-ports` 工具

### 參考資源

- [VS Code Remote Development](https://code.visualstudio.com/docs/remote/remote-overview)
- [SSH Port Forwarding](https://www.ssh.com/academy/ssh/tunneling/example)
- [CORS MDN Documentation](https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS)

---

**文檔版本**: 1.0.0  
**最後更新**: 2025-12-04  
**維護者**: WebSecScan Platform Team
